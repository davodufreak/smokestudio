# Prompt de Corrección — SmokeStudio R2
> Generado: 2026-04-23 00:44 | Rama: SmokeStudio.Rev01

Copia y pega este prompt en Claude Code para aplicar todas las correcciones detectadas en el reporte de QA.

---

## PROMPT

```
Eres un ingeniero de software senior. Debes corregir los bugs y vulnerabilidades de seguridad detectados en el reporte de QA del proyecto SmokeStudio R2 (rama SmokeStudio.Rev01). Aplica TODOS los cambios listados a continuación en el orden indicado. No agregues features, refactors ni abstracciones que no estén en esta lista. Haz commit solo cuando yo lo pida.

### ARCHIVO: smokescan.js

[FIX 1 — CRÍTICO] XSS en chatbot (línea ~1486)
En la función del botón btnSendChat, reemplaza la línea:
  chatWindow.innerHTML += `<div style="...">${txt}</div>`;
Por código que crea el elemento con document.createElement('div'), asigna
el texto con element.textContent = txt, aplica los mismos estilos inline
como propiedades de element.style, y usa chatWindow.appendChild(element).

[FIX 2 — CRÍTICO] getScoreStatus() no definida (línea ~1183)
En applyChangesAndReanalyze(), dentro del setTimeout de 1200ms, reemplaza:
  const newResult = getScoreStatus();
Por:
  const newResult = generateSimulatedResult();

[FIX 3 — ALTA] saveAnalysis() nunca se llama
Al final de la función showResults(result), antes de su llave de cierre,
agrega la llamada:
  saveAnalysis();

[FIX 4 — ALTA] updateBreakdown definida dos veces
Elimina COMPLETAMENTE la primera definición de updateBreakdown (líneas ~132-144),
la que usa esta lógica interna:
  const scoreEl = $(`${prefix}bd-${cat}-score`) || $(`${prefix}rbd-${cat}-score`);
Conserva únicamente la segunda definición (~línea 773), que empieza con:
  const p = prefix ? `${prefix}bd-` : 'bd-';

[FIX 5 — ALTA] initReset() no resetea step0 (línea ~1338)
En initReset(), en el objeto de reset de STATE.answers, agrega step0:
  step0: { selection: null, desc: '', recursos: { capital: '', inversores: '', aliados: '', equipo: '' } },
al inicio del objeto, antes de step1.

[FIX 6 — MEDIA] Sanitizar campos de Gemini en innerHTML
En populateResults(result), agrega esta función al inicio:
  const sanitize = s => (s || '').replace(/</g, '&lt;').replace(/>/g, '&gt;');
Luego envuelve con sanitize() todas las interpolaciones de datos del resultado
de IA que van dentro de innerHTML: cat.name, cat.desc, step.title, step.description.

[FIX 7 — BAJA] Comentario Firebase incorrecto (línea ~1586)
Reemplaza:
  initAuth(); // New Firebase Auth initialization
Por:
  initAuth();

### ARCHIVO: main.js

[FIX 8 — BAJA] Null-check en navWrapper (línea ~7)
Envuelve el addEventListener de scroll en un if (navWrapper) { ... }
para evitar crash en páginas donde el elemento no existe.

### ARCHIVO: smokescan.html

[FIX 9 — MEDIA] Etiqueta </section> huérfana (línea ~146)
Elimina el </section> duplicado que aparece entre el cierre de la sección
hero y el inicio de la sección "Cómo funciona".

[FIX 10 — BAJA] Typo en copy (línea ~190)
Reemplaza "contrar tu estragia de lanzamiento"
por "construir tu estrategia de lanzamiento".

### VERIFICACIÓN FINAL
Después de aplicar los fixes, confirma:
1. getScoreStatus no aparece en ningún lugar del código.
2. saveAnalysis() se llama exactamente una vez desde showResults().
3. updateBreakdown tiene exactamente UNA definición en smokescan.js.
4. Las etiquetas <section> en smokescan.html están balanceadas.
5. No hay errores de sintaxis JS (llaves desbalanceadas, etc.).

No hagas commit hasta que yo lo confirme explícitamente.
```

---

*SmokeStudio R2 — 2026-04-23 00:44*
